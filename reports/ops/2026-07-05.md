# Ops digest — 2026-07-05

First run — no prior digest to diff against, so "watching" items below are
baselines for tomorrow, not yet-escalated repeats.

## Needs action

- **PR #1 on `agentops-recruitment-workflow` looks like it's in the wrong repo.**
  ["Add WhisperFlow: local Wispr Flow clone for macOS"](https://github.com/MossAbid/agentops-recruitment-workflow/pull/1)
  (opened 2026-07-05T14:15Z, branch `claude/whisperflow-local-clone-fn7tx9`) adds a
  local macOS speech-to-text dictation tool — unrelated to the recruitment agent
  this repo hosts. No CI checks are configured on it (commit status: 0 checks),
  so nothing will catch drift while it sits open. Worth confirming whether it
  belongs here or should move to its own repo.

## Watching

- **Two branches are stale (>14 days, no open PR):**
  - `claude/toddler-alphabet-numbers-app-66GQy` (agentops-recruitment-workflow) —
    last commit 2026-06-07, 28 days old.
  - `claude/perf-lab-rowing-sub-ilaj9p` (perf-lab) — last commit 2026-06-10, 25 days old.
  Neither has an open PR. Safe to delete if abandoned, or open a PR if still wanted.
- **perf-lab Pages workflow has two runs stuck in `queued`** from 2026-07-02
  (20:51 and 22:01) that never completed — matches the known "déploiement coincé
  en file" pattern from repo history. Not currently a problem: later runs the same
  day and on 2026-07-04 completed successfully. Flagging in case it recurs.
- **`requirements.txt` (agentops-recruitment-workflow) pins no versions**
  (`fastapi`, `uvicorn`, `python-dotenv`, `langchain`, `langgraph`, `agentops`,
  `mistralai`). No baseline yet to check for a breaking release since "last
  digest" — future runs will diff against this list.

## All clear

- perf-lab: latest GitHub Pages deploy (2026-07-04T19:35:53Z) succeeded.
- perf-lab: `sw.js` `CACHE` is already at `perflab-v8`, bumped in the same commit
  (`e2ccf37`, current `main` tip) that changed shell files — no missing bump.
- No open issues on either repo.
- `fable5-rpe-profiles` (perf-lab, last commit 2026-07-02) is 3 days old — under
  the 14-day threshold, not stale yet.
